import Hero from '../components/Hero';
import Body from '../components/Body';

export const index = () => {
    return (
        <div>
            <Hero />
            <Body />
        </div>
    );
};

export default index;
